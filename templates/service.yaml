apiVersion: v1
kind: Service
metadata:
  name: {{ include "vision.fullname" . }}
  labels:
    {{- include "vision.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vision.selectorLabels" . | nindent 4 }}
