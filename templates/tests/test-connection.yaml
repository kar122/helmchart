apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vision.fullname" . }}-test-connection"
  labels:
    {{- include "vision.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vision.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
